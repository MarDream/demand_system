1. 修改 `demand_frontend/src/views/requirements/create.vue` 的表单顺序：将动态扩展字段区块移动到“需求描述”表单项之前，使扩展字段从需求描述上方开始；保留现有 `DynamicFieldsForm`、校验和提交数据逻辑不变。
2. 调整开始时间默认值：新增需求初始化时，将 `formData.startDate` 设置为当前日期的 `YYYY-MM-DD`；编辑需求时继续优先使用后端返回的 `data.startDate`，避免覆盖已有开始时间。若当前流程节点时间可从详情字段获得，则优先使用该节点流转时间，否则回退到当前日期，并在模板上保持可编辑。
3. 优化右侧“基础信息”编辑布局：在基础信息卡片内部增加双列自适应布局（桌面端每行 2 个控件，窄屏自动单列），让所属项目、负责人、抄送人、所属迭代、当前状态按网格排列；时间卡片维持现有结构，避免影响日期和工时字段的显示条件。
4. 补充/调整对应 CSS，确保双列布局下 `el-form-item` 宽度、跨列字段和移动端响应式表现正常，并与现有页面间距及卡片样式一致。
5. 运行前端类型检查或构建，检查模板、样式和默认值逻辑无错误；最后汇总修改文件与验证结果。