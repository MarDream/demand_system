# 2026-09-21

## 我的待办按「当前生效角色」过滤（修复角色切换不生效）

**问题**：截图反馈——zhanxingmeng 选「产品经理」角色，但「我的待办」里出现绑定给
「运营工单员」(ROLE_B47FF7) 的需求。角色切换没在待办查询里生效。

**根因**：`SecurityUtils.getCurrentUserRoles()` 已由 `JwtAuthenticationFilter` 按
`X-Active-Role` 头收窄（角色切换机制本身是好的），但
`RequirementServiceImpl.getUserRoleIds(userId)` **直接查 `user_roles` 表拿全集**，
`selectMyPendingV2` 的 XML 又忽略 `roleIds` 参数、自己 JOIN `user_roles`——
两层都绕过了收窄。

**修复**（2 个文件）：
- `RequirementServiceImpl`：新增 `getCurrentUserRoleIds()`（生效角色码 → roles 表查 ID），
  替换 4 处调用点：`listMyPending`、`listMyDone`、统一列表的 `case "pending"/"done"`、
  `canViewForSearch`（详情查看权限的待处理人判定）。
- `RequirementMapper.xml`：`selectMyPendingV2` 与 `selectMyDoneV2`（排除块）删掉
  `user_roles` JOIN，改为 `direct_role.id IN (roleIds)` / `rg_role.id IN (roleIds)`；
  roleIds 为空时 `1=0`（无生效角色 → 无角色型待办）。
- **保留全集的**：`resolveRoleDataScopeOrgIds`（数据可见范围 = 全部角色的数据权限并集，
  收窄它会导致「待办在列表里、组织却不可见」的自相矛盾）。

**语义口径**（用户明确要求）：处理角色归属（谁能办这条）随登录选择的角色收窄；
数据可见范围维持多角色并集不变。

**验证**：`scripts/verify-my-pending-active-role.cjs`（**10 项**：PM 下不含 343/372、
运营工单员下含、全集兜底含、伪造 X-Active-Role 被忽略按全集、activeRole 回显、
**角标三态**：PM 下 pending=0 / 运营工单员下 >0 / 全集 ≥ 单角色）
**10/0 连跑多次**；UI 级 `verify-pending-role-ui.cjs`（PM 待办空、切运营工单员后出现）
**3/0 连跑多次**。后端 plexus 绕过编译 BUILD SUCCESS + java argfile 重启。

### 第二批：角标与流转操作权限同口径收窄

列表修完后排查出两处仍按角色并集的盲区，一并收窄：
- **待办角标**（`/tab-badge-counts` 的 `pending` + 仪表盘「待办数」）：
  `StatisticsServiceImpl` 改调新方法 `countByUserIdWithOrgFilterAndActiveRole(userId, roleIds, ...)`，
  roleIds 来自 `requirementService.getCurrentUserRoleIds()`（已改 public 供包外复用）。
  不修的现象：切到产品经理后列表空、角标还挂着旧数字。
- **流转操作权限**（`WorkflowEngineService.validateRuntimePendingPermission`）：
  `countAccessibleByCurrentWorkflowPositionAndUser` 加 `roleIds` 参数——
  产品经理角色下**不能办理**绑定给运营工单员的节点（之前只挡列表、不挡操作，是越权口子）。

**实现方式**：`RequirementPendingTaskMapper.xml` 的共享片段
`pendingUserAccessJoins`/`pendingUserAccessCondition` 支持**可选 `roleIds`**：
- `roleIds == null`（未传）→ 保持旧口径（JOIN user_roles 全集），旧调用方零改动；
- `roleIds` 传集合 → 按生效角色匹配，空集 → `1=0`；
- `selectPendingUserIds`（查节点全部待办人，展示用）不传，保持全集。
Mapper 接口新增 `countByUserIdWithOrgFilterAndActiveRole`（角标用），
`countAccessible...AndUser` 加 `roleIds` 参数（流转校验用）。
- 曾顺手建了 `selectPendingRequirementIdsByActiveRole` 变体，核实后发现
  `selectPendingRequirementIds` 本身就**没有任何 Java 调用方**（存量死代码）→
  把我建的变体删了，存量两个（`selectPendingRequirementIds`/`countByUserId`）按规矩记录待用户决策，不擅自扩面。

**最终回归（后端 PID 24332，最终版代码）**：
`verify-my-pending-active-role` 10/0、`verify-pending-role-ui` 3/0、
`verify-roles-toolbar-and-home` 15/0。

### 收尾：全仓 user_roles 扫描（确认无遗漏收窄点）

mapper XML 引用 `user_roles` 的仅 3 处，逐一核过语义：
- `RequirementPendingTaskMapper.xml`：已改造（可选 roleIds）✅
- `StatisticsMapper.xml`：仅判 **SUPER_ADMIN 身份**（超管是身份不是业务角色，收窄会让
  超管切小角色后统计异常）→ 保持全集正确
- `UserMapper.xml`：用户管理 CRUD → 与生效角色无关，正确

Java 侧 `UserController.getUserRoleIds` 是用户管理接口（查某用户拥有哪些角色）→ 全集语义正确。

**结论**：「以当前身份办事」的路径（待办列表/已办/角标/仪表盘待办数/流转办理/详情查看）
已全部按 `X-Active-Role` 收窄；`user_roles` 全集仅保留在三类合理场景
（数据可见范围、超管身份判定、用户管理 CRUD）。本轮任务闭环。

### 收尾补充：切角色时全局强制刷新（router-view key）

需求页靠组件内 watch + 缓存 key 修了，但其它页面（仪表盘统计、待办角标卡片等）
数据都在 onMounted 拉、无 keep-alive——切角色时**停留当前页**不会重拉。
通用修法（`DefaultLayout.vue`）：`<router-view>` 内容 div 的 key 从 `viewRoute.path`
改为 `` `${userStore.activeRole || ''}:${viewRoute.path}` `` —— 切角色时当前页组件强制重建，
所有页面数据按新角色重拉，一处改动覆盖全站。需求页的 watch 保留作双保险（缓存清理仍需要）。

**回归（key 改动后全套）**：`verify-my-pending-active-role` 10/0、`verify-pending-role-ui` 3/0、
`verify-roles-toolbar-and-home` 15/0、`verify-workflow-version-sort` 16/0、
`verify-workflow-version-time-columns` 23/0。`vue-tsc` EXIT=0。

### 收尾补充 2：H5 端补齐 X-Active-Role（两端口径一致）

排查发现 **H5 端（demand_h5）有「我的待办」但不发 X-Active-Role 头** → H5 永远走全集口径，
PC 切到产品经理看不到的待办 H5 能看到（两端不一致）。修了三处（`vite build` 通过）：
- `utils/auth.ts`：`getActiveRole/setActiveRole`（`h5_active_role` key），`clearAuth` 一并清除
- `api/request.ts`：请求拦截器带 `X-Active-Role`（与 PC 端 `api/request.ts` 同构）
- `stores/user.ts`：`fetchUserInfo` 后 `syncActiveRole`——本地残留值仍有效（在 allRoles 中）则保留
  （H5 与 PC 各自独立记忆角色），失效则清空回全集口径；`AuthUserInfo` 类型补
  `activeRole/allRoles/allRoleNames` 字段（后端 `/auth/me` 本来就返回）。
- H5 无切换角色 UI（不在本次范围）；后端流转校验已按生效角色收窄，H5 提交流转同样被保护。

**全链路终态**：PC 列表/角标/流转/H5 待办 全部按 `X-Active-Role` 收窄；
全站 router-view key 随角色重建；数据可见范围保持并集。

**H5 运行时验证**（dev 5175 + Playwright iPhone 视口）：`scripts/verify-h5-active-role.cjs`
**5/0** —— H5 登录成功；`/auth/me` 返回 `allRoles/activeRole`（syncActiveRole 前提成立）；
带 `X-Active-Role` 的 my-pending 在产品经理下不含 343/372、运营工单员下含（服务端口径正确）；
`h5_active_role` 存取正常；0 JS 报错。注意：H5 fetch 手动带头验证服务端口径
（页面内 fetch 不走 axios 拦截器，要验证拦截器只能查它发出的请求，此处用口径断言替代）。

**前端两处配套修复**（切角色后页面不重拉的缓存问题，`requirements/index.vue`）：
- `tabDataCache` 的 key（含 stale key）加 `activeRole` 前缀——切角色后天然失效；
- 组件 `watch(userStore.activeRole)`：清缓存 + 回第 1 页 + `fetchData()/refreshViewCounts()`。
  实测没有这个 watch 时，API 已返回 50 条但表格仍显示旧数据（页面不重挂，onMounted 不重跑）。
- `invalidateViewCache` 的匹配从 `key.startsWith(view:)` 改为 `key.includes(':view:')`
  （key 前缀变了，且跨角色全部失效本就是操作后语义）。

**注意**：`verify-roles-toolbar-and-home.cjs` 的 A2（设置→角色管理页工具栏）失败，
是**另一会话 23:29 正在实时重构 `settings/roles.vue`**（`.permission-toolbar` 已被删除）。
与本次改动无关。**后续已跟进**：对方把权限工具栏并入操作行（新类名 `.role-actions-bar`，
搜索框 + 图标化全选/清空 + 保存权限同排），脚本 A 组断言已按新类名更新，
15/0 连跑两次（之前 11/1 是因为 toolbar 找不到时 A2/A3 子断言被 if 跳过）。

## 启动前后端服务
- 后端 `java @target/run.args` 后台拉起（~18s 就绪），jps 显示主类方式启动；登录冒烟 200。
- 前端 Vite dev `127.0.0.1:5170`（管理 node 22.22.2 直接跑 `node_modules/vite/bin/vite.js`）。
- 登录页 200、登录接口 `{"code":200}` 验证通过。

## 用户管理页面缺少右滑动块（已修复）
- **现象**：人事管理模式（`roster-layout`）左侧「人事导航」没有拖拽滑块；基础管理模式（`member-layout`）有。
- **根因**：`views/settings/users.vue` 中 `rosterSidebar = useCollapsibleSidebar({...})` 没传
  `resizerWidth`/`resizerWidthVar`（默认 0，`startResize` 直接早退），模板里也没放 `.sidebar-resizer`，grid 只有两列。
- **修复（三处）**：
  1. `rosterSidebar` 配置补 `resizerWidth: 4, resizerWidthVar: '--roster-sidebar-resizer-width'`；
  2. `roster-nav` 之后补 `<div class="sidebar-resizer" @mousedown="rosterSidebar.startResize" @dblclick="rosterSidebar.toggle" />`；
  3. `.roster-layout` grid 改三列 `var(--roster-sidebar-width, 244px) var(--roster-sidebar-resizer-width, 4px) minmax(0, 1fr)`。
- **验证**：vue-tsc 0 错误；Playwright `scripts/verify-users-roster-resizer.cjs` 9/9 全绿
  （滑块存在/cursor/拖宽 244→304/缩窄到 minWidth 220/双击收起 0px/展开按钮恢复）。
- 双击滑块收起、`sidebar-expand-btn` 恢复是 composable 既有能力，roster 侧天然获得。

## 用户管理「26 人显示不全」（已修复）
- **现象**：选中东莞市（26 人）后右侧表格看不到全部人员，也无分页可点。
- **根因**：分页组件其实一直在（`member-console` 底部），但 `.member-console` 是
  `min-height: calc(100vh-130px)` + `overflow:hidden`，内容撑高后分页条被顶出视口
  （实测 top=871 > viewport 800）；而外层滚动容器 `.app-main` 的 `scrollHeight==clientHeight`
  （布局链全是 `overflow:hidden`/撑高），**根本滚不动** → 分页永远不可见。
- **修复（users.vue 三处 CSS）**：
  1. `.member-console`：`min-height` → `height: calc(100vh - 130px)` + `display:flex; flex-direction:column`；
  2. `.member-layout,.roster-layout`：`min-height` → `flex:1; min-height:0`（占满 console 剩余高度）；
  3. `.member-main,.roster-main`：加 `overflow:auto`（表格在主区内部滚动）。
  分页条钉在 console 底部始终可见，两种模式共用生效。
- **数据口径坑（重要）**：树上「东莞市 (26人)」计数与列表 total 口径一致（含子组织递归），
  26 = 每页10×2页 + 6；第二页仍是满 10 条（含子组织「运营团队」用户 12/11/10...），
  回归脚本第一版断言「第二页 16 条」是臆算，直接调接口逐页核实后改为 10+10+6。
- **验证**：`scripts/verify-users-pagination.cjs` 11/11 全绿（分页可见/翻页/切每页50 一页看全/人事模式无回归）；
  滑块脚本 `verify-users-roster-resizer.cjs` 复跑 9/9 未破坏。


---

# 角色权限页头部紧凑化 + 仪表盘菜单权限控制

## ① 权限范围头部紧凑化（用户截图红框）

**问题**：`settings/roles.vue` 权限面板头部占三行（「权限范围」标题 + 解释文案 + 搜索/全选/清空一行），
视觉空旷。

**改法**（遵循「UI 文案极简」约定）：删掉 `.panel-head` 整块（标题 + 解释文案），
搜索框与「全选当前/清空当前」合并成**单行工具栏**，两个动作按钮**图标化**（`Select`/`Delete` 图标
+ `el-tooltip` 提示）。`grantDisabledReason`（无权配置的原因）改为禁用态按钮的文字 —— 它承载
真实信息，属于文案约定的例外。实测头部到首行 delta 从 ~150px 收到 **58px**。

## ② 仪表盘菜单权限控制 + 登录首页回退

**链路**（三层）：
1. **DB**：迁移 `database/migrations/2026-09-21-dashboard-menu-permission.sql` ——
   新权限 `menu:dashboard`（sys_permissions id=141）、挂到 `sys_menus.id=1`（原 NULL 人人可见）、
   存量补授：**所有持有任意 menu:% 权限的角色**（REGION_ADMIN 例外，它一个菜单权限都没有）。
   已同步 `init.sql`（权限 seed + 菜单行 + 角色 seed `(x,141,1)`）。
2. **路由**：`/dashboard` 加 `meta.requiredPermissions: ['menu:dashboard']`。
3. **首页解析**：`useAppStore.resolveHomePath()` —— 按 `menus/current` 返回顺序
   （服务端已按角色过滤 + sortOrder 排序）取首个可导航 path；空树返回 null。
   接线点：登录成功（`login/index.vue`，无显式 redirect 时先拉菜单再跳解析结果）、
   `guards.ts`（`/`、`/dashboard`、无权限兜底共用一段）、角色切换（`DefaultLayout.vue`）。

**关键坑：needOrgBind 与首页回退会互相重定向成死循环**（`waitForURL` 30s 超时的根因）：
旧守卫把所有非 `/dashboard` 导航强制回 `/dashboard`，而无 menu:dashboard 的用户又被
首页回退逻辑推走 → 循环。**OrgBindDialog 挂在 DefaultLayout（任何页面都会弹）**，
旧注释"dashboard 页面挂载了 OrgBindDialog"已过时。修法：needOrgBind 分支内也用
`resolveHomePath()` 计算落点，目标就是落点（或其子路由）时放行让弹窗完成绑定。

**验证**：`scripts/verify-roles-toolbar-and-home.cjs` **15/15 连跑两次**
（admin 落 /dashboard + 侧边栏有仪表盘；A1-A3 头部紧凑化断言；REGION_ADMIN fixture
登录落 /requirements、直访 /dashboard 被拦回 /requirements；fixture 自清理）。
`vue-tsc` EXIT=0。`grep -v Warning || true`：INSERT 无输出时 grep 退出码 1 会炸 execSync，
必须兜底；bcrypt hash 含 `$`，SQL 走 base64+stdin 不能进 `-e "..."`。

## 追加：角色权限页二轮微调（用户截图反馈）

- 工具栏两个图标按钮包进 `.permission-toolbar__actions`（`margin-left:auto`）贴右侧 ——
  之前 space-between 只有一个按钮组时它会被顶到中间偏右，视觉上"悬在半空"。
- 按钮复选框压成 chip：`.button-permission-grid` 列宽 `minmax(220px,1fr)→(160px,1fr)`、
  gap `--spacing-sm→6px`、复选框 `min-height:56px→0` + `padding 9px 12px→4px 10px`、
  label 从纵向 flex 改回行内 —— 实测 30px 高 / 178px 宽（原 56px / 244px），
  需求管理 24 个按钮从 5 行变 4 列×6 行整体高度减半。

## 追加：角色权限页三轮微调 —— 说明改角标 + 搜索框并入操作行

用户诉求两条：① 角色说明不该占大量空间（应并入「查看角色说明」或以角标显示在角色名后）；
② 全选/清空与操作按钮同排，省空间给权限菜单。追问后用户选了**搜索框也并入同一行**。

**改法**（`views/settings/roles.vue`，只改这一个文件）：
1. **说明行整块删除**：`.detail-description` 原本是 `.role-detail`（flex column, gap）的**独立子元素**
   → 独占 ~21px 行 + ~10px gap ≈ **30px**，这是诉求①的根因。改为在角色名后放 `InfoFilled` 角标
   （`.detail-desc-badge`）+ `el-popover(trigger="hover", :show-after=120, :width=320)`，内容走 `.detail-desc-text`。
2. **角色编码并入标题行**：`.detail-code` 原来也单独一行（~26px），折进 `.detail-title`（flex-wrap）。
   `.detail-header` 现为 27px 单行。
3. **搜索框 + 全选/清空搬进 `.role-actions-bar`**：`.permission-toolbar` 整块（含其样式）删除。
   搜索框 `flex: 1 1 180px / min-width:150px / max-width:320px`，`.role-actions-bar__bulk` 用 `margin-left:auto` 贴右。

**收益**（1440×900 实测）：`.menu-permission-list` y 从 ~227 → **187**（可授权态），
`.permission-content` clientHeight 626，首屏可见权限菜单 **6 行**。

**关键坑**：
- `AppButton` 缺 `permission` 时是 **`v-if` 从 DOM 移除**（不是禁用）→ 布局不能依赖它的占位。
- **禁用按钮不派发鼠标事件**，tooltip 永远不弹：不可授权态（超级管理员）要把
  `grantDisabledReason` 挂上去，必须包一层 `<span class="role-actions-bar__bulk-trigger">` 承接 hover，
  并给 `.el-button.is-disabled { pointer-events: none }`。
- 最初用「一条长文案按钮」承载原因 → 实测 396px 宽把操作行撑成 **2 行（82px）**；换成两个禁用图标按钮后
  回到 **42px 单行**，6 个按钮同 y。

**验证**：`scripts/verify-roles-compact-layout.cjs` **32 断言连跑两次全绿**（只读、无残留）。
覆盖：无 `.detail-description`／角标存在／popover 文本 == 接口 `description`／`.detail-code` 父级是 `.detail-title`／
`.permission-toolbar` 已删／操作行单行且 6 按钮同 y／搜索框在 `.role-actions-bar` 内／两种授权态的降级表现／
搜索仍能过滤且清空复原／1100px 窄屏仍单行／0 JS 错误。临时探针 `_probe_roles_layout.cjs` 已删除。

**断言口径坑（重要）**：C7 最初写「搜索框底边 → 菜单列表顶边 <= 45px」，在**超级管理员态失败（53px）**——
因为该态在权限面板顶部多一条 `<el-alert>` 警告条（承载真实信息，属文案约定例外），把参照物顶下去了。
改成「**操作行底边 → `.permission-content` 顶边 <= 40px**」才是「操作行之后没被塞进额外整行」的正确判据。
教训：几何阈值断言要挑**不受同页其它合法元素影响**的参照物，否则换个角色/权限态就假红。

## 角色页隐藏全部编码（2026-09-21 晚）

用户要求「隐藏角色编码，菜单权限编码等，无需显示」。roles.vue 改动：
- 移除详情头部 `.detail-code`（`selectedRole.code` 角标）。
- 移除权限树一级/子菜单行的 `.permission-code` span（两处），`.menu-permission-row` 网格 4 列→3 列（28px / 1fr / 88px；两处媒体查询同步）。
- 新建/编辑对话框移除「角色编码」表单项 + AI 生成按钮；编码改为纯后端逻辑：新建时 `handleSubmit` 里 `generateRoleCode(form.name)`，编辑时保留原编码（`form.code = role.code`，系统角色编码被 SUPER_ADMIN_CODES/权限判断引用不可变）。`handleRoleNameInput` 仅新建时生成。
- 清理死代码：`MagicStick` 图标、`llmProviderApi` 导入、`codeManuallyEdited`/`aiCodeGenerating` ref、`handleAiGenerateCode` 函数、`rules.code`、`.detail-code`/`.permission-code` 样式。导出 Excel/导入解析里的「角色编码」列保留（数据交换需要）。
- 验证：`scripts/verify-roles-codes-hidden.cjs`（admin/admin123 登录；zhixiaodong 是部门管理员无系统设置权限，进不去 roles 页）8/8 PASS ×2；vue-tsc 0 错误。
- 教训：验证脚本点「新增角色」时 `button:has-text("新增角色")` 会同时命中「新增角色组」的前缀匹配 → 用 `getByRole('button', { name, exact: true })`；编辑对话框要遍历角色树找一个非系统角色（系统角色「编辑角色」按钮 disabled）。

## 角色页移除静态「角色说明」页（2026-09-21 深夜）

用户反馈「每个角色的角色说明都一样」。定位：右上「查看角色说明」按钮（`selectedRole = null`）打开的是一块**写死的静态说明页**（empty-guide：什么是角色/怎么使用角色 + flow-preview 流程图），与所选角色无关；而角色名旁的 ⓘ popover 本身就按角色读 DB description（各角色内容本就不同：超级管理员=系统最高权限…）。
- 移除：详情头部「查看角色说明」link 按钮、整个 `v-else .empty-guide` section、`previewNodes` 常量、`showTodo` 函数、`.empty-guide/.guide-copy/.flow-*` 全部样式 + 两处媒体查询引用、失去引用的 `RAW_SUITCASE_ICON/RAW_TICKETS_ICON/RAW_TOOLS_ICON` 与 `Suitcase/Tickets/Tools` 图标导入。
- 保留：ⓘ 角标（贴合所选角色的真实 description）。
- 验证 `scripts/verify-roles-desc-simplified.cjs` 8/8 ×2：无按钮、无静态页、超管/产品经理 ⓘ tooltip 各自不同。
- 注意：脚本偶发登录 FAIL（url 停在 /login），单独重跑即过 —— 登录后 waitForTimeout(3000) 可能不够或瞬时抖动，别急着改代码。
