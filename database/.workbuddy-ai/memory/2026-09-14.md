# 2026-09-14

## 数据库 SQL 整合：全部合并进 init.sql

将 `database/` 下 38 个 SQL（migrations/ 34 个 + 4 个独立脚本）整合进 `database/init.sql`，
之后删除全部冗余文件。`init.sql` 现为唯一入口（95 张表，2908 行）。

备份：`E:/Project/Vue_demo/demand_system/database_sql_backup_20260914.zip`（38 个文件）。
另：34 个文件中 32 个已被 git 跟踪，可 `git checkout` 恢复；未跟踪的
`V20260913_01`、`V20260914_01` 仅存在于上述 zip。

### 合并前 init.sql 已有的内容
init.sql 原本已是「当前库快照 + 部分迁移补齐区」（第 56~59 节），
所以真正缺失的只有 6 项，不要重复合并：

1. `bitable_automations.last_fired_at` 列（V20260912_01，原靠动态 PREPARE 补齐）
2. `bitable_cell_values` 三个复合索引 `idx_field_value_number/date/text`（V20260716_03）
3. `/settings/assistant` AI 助手设置菜单（V20260807_02）
4. `/settings/git/repositories` 仓库管理菜单（V20260809_01）
5. 进阶工作流模板 tpl-2.0.0~tpl-2.5.0 种子数据（V20260805_02，371 行）
6. `protection_rule_sets` 两条种子数据（code_management_init.sql）

### 顺带修掉的问题
- 第 58 节（Git 模块 6 张表）中文注释是 cp1252 乱码（mojibake），且部分字节
  已变成 C1 控制字符无法逆向还原 → 直接用 `code_management_init.sql` 的干净 DDL
  整节替换。全文件乱码行现为 0。

### 校验方式（无 MySQL 可用时）
用 sqlparse 做静态校验：括号/引号配平、CREATE TABLE 无重复、FK REFERENCES 目标表
均存在、迁移的 ADD COLUMN / CREATE INDEX 全覆盖。docker 起 MySQL 验证需审批，本次未做。

### 遗留提醒
- 迁移目录已删除，已部署环境失去升级路径，后续 schema 变更直接改 init.sql。
- 原 migrations 存在版本号冲突：`V20260911_01` 有两个不同文件
  （add_suggested_follow_ups / drop_custom_field_visible_statuses），Flyway 会报错。
  删除后此隐患消失。
