<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demand.system.module.relation.mapper.RequirementRelationMapper">

    <!-- 查询需求关联及目标需求信息 -->
    <select id="selectWithTarget" resultType="java.util.Map">
        SELECT rr.*,
               r.title as target_title,
               r.type as target_type,
               r.status as target_status,
               r.priority as target_priority
        FROM requirement_relations rr
        LEFT JOIN requirements r ON rr.target_id = r.id
        WHERE rr.source_id = #{sourceId}
    </select>

    <!-- 检查需求关联是否存在 -->
    <select id="exists" resultType="int">
        SELECT COUNT(*)
        FROM requirement_relations
        WHERE source_id = #{sourceId}
          AND target_id = #{targetId}
          AND relation_type = #{relationType}
    </select>

</mapper>
