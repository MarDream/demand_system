<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demand.system.module.bitable.mapper.BitableAutomationRunMapper">

    <!-- 按自动化规则ID分页查询执行记录 -->
    <select id="selectByAutomationId" resultType="com.demand.system.module.bitable.entity.BitableAutomationRun">
        SELECT id, automation_id, event_id, status, trigger_detail, action_result,
               error_code, error_message, attempt, started_at, finished_at, created_at
        FROM `bitable_automation_runs`
        WHERE automation_id = #{automationId}
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 统计指定自动化规则的执行记录数量 -->
    <select id="countByAutomationId" resultType="int">
        SELECT COUNT(*)
        FROM `bitable_automation_runs`
        WHERE automation_id = #{automationId}
    </select>

    <!-- 查询指定自动化规则的最近一次执行记录 -->
    <select id="selectLatestByAutomationId" resultType="com.demand.system.module.bitable.entity.BitableAutomationRun">
        SELECT id, automation_id, event_id, status, trigger_detail, action_result,
               error_code, error_message, attempt, started_at, finished_at, created_at
        FROM `bitable_automation_runs`
        WHERE automation_id = #{automationId}
        ORDER BY created_at DESC
        LIMIT 1
    </select>

</mapper>
