<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demand.system.module.bitable.mapper.BitableViewMapper">

    <!-- 按数据表ID查询所有未删除的视图 -->
    <select id="selectByTableId" resultType="com.demand.system.module.bitable.entity.BitableView">
        SELECT id, table_id, name, view_type, sort_config, filter_config, group_config,
               column_config, color_config, sort_order, created_by, created_at, updated_at
        FROM `bitable_views`
        WHERE table_id = #{tableId}
          AND deleted_at = 0
        ORDER BY sort_order ASC, created_at ASC
    </select>

    <!-- 统计指定数据表下的视图数量 -->
    <select id="countByTableId" resultType="int">
        SELECT COUNT(*)
        FROM `bitable_views`
        WHERE table_id = #{tableId}
          AND deleted_at = 0
    </select>

</mapper>
