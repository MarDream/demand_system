<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demand.system.module.llm.mapper.LlmModelMapper">

    <!-- 查找默认的 Chat 模型（isDefault=true 且 enabled=true 且 modelType 不是 embedding/rerank）。
         按更新时间倒序取第一条。 -->
    <select id="selectDefaultChatModel" resultType="com.demand.system.module.llm.entity.LlmModel">
        SELECT * FROM llm_models
        WHERE is_default = 1
          AND enabled = 1
          AND model_type NOT IN ('embedding', 'rerank')
        ORDER BY updated_at DESC
        LIMIT 1
    </select>

</mapper>
