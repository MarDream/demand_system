有以下需求补充，请帮我创建Agent Teams模式来完成需求的优化
产品经理：充分理解需求描述，对用户提出的需求进行调研细化确认，最终形成明确需求书传递给开发团队
前端开发2名：根据产品经理的需求描述及现有需求建设情况，对产品原型进行设计打磨，给出精细的原型设计，最终根据设计对前端代码进行实现
后端开发：依据前端产品原型设计，对代码逻辑进行修改，并追求代码实现性能的最优
测试人员：进行集成测试和产品体验官，对不合理的用户体验提出质疑，反馈产品经理重新调整设计方案
一、需求配置
  1、对需求的类型进行管理，因为需求可以是工单类型用于处理日常例行的数据问题、接口对接、测试等内容，也可以是需求工单，用于标识零散的开发需求，即，需求类型其实是对需求内容的标签化定义，建议设计为维护
    类型字典值，为有效维持需求类型的扩展性，需求类型支持新增、删除、编辑，是全局性的。
  2、节点状态：节点状态用于对工单进行流转，每个节点可以分给不同的角色或指定具体的人进行处理，节点的初始状态根据工作流的设定定义，节点状态可以自定义多个，如待分析、待确认、待评审、待排期、开发中、测试中、已上线、已验收、取消等状态，
    当然根据业务需求的变化，可以继续新增或删除相应的节点。
  3、节点流转，即描述的是每个节点的下个节点是什么，这种前后置依赖关系是通过工作流配置完成的。
  4、优先级：即描述的是具体需求类型的紧急程度，是个全局的字典值，需要支持对优先级拖拉排序，但不要显示拖拽图标，显示一个手抓的图标时即可进行排序调整，排序顺序用于在需求管理中新建需求时标识需求工单的紧急情况，按照排序顺序显示，供用户进行选择。

二、工作流配置
  1、新建工作流：从开始节点开始，选择节点状态，通过拖拉拽图标，并设定图标绑定的节点状态，然后图标与图标之间进行前后置关系的连线，以此来设计工作流的流转路线，节点间允许回退，但仅能回退到上一个节点；
  2、节点设置：每个节点要绑定节点对应的状态，还需要绑定角色或人员，绑定角色时，表示只要拥有该角色的人员，即可对该节点的需求可见且可进行流转，需求的流转就是争取式的，谁先流转了，就记录为谁处理的。如果绑定的是具体的人，则表示只有这个人才能进行流转；
  3、节点流转：节点的流转支持退回，即，只要工作流未结束，则流程可以向他的上一节点进行回退，直到回退到开始节点，开始节点即为需求的创建节点，此节点时需求状态为新建，新建完成后，可以点击提交，则流转至下个节点，也可以保留草稿。
  4、特殊节点说明：开始节点可以直接选择流转至取消，默认是流转至工作流配置的下个节点，取消则流程直接结束；除结束节点不能流转到取消节点外，其他节点均能流转至取消节点，但只有指定的角色可以选择流转至取消；当流转至结束节点时，该节点表示整个工作流结束，不能再进行回退或流转操作。
  5、流转记录：每个节点的流转，均需要记录具体的流转人员、流转开始时间、结束时间、停留时间（时间从秒、分、时、天、月、年，按照实际情况优雅显示），每个需求均需要有完整的流转记录
三、组织架构管理
  1、区域、公司、部门、岗位（岗位即角色）均能单独进行增、删、改，且能按照折叠的树形结构进行管理，默认是折叠状态；
  2、用户管理：用户名，默认只能是字母、数字、下划线等组合，不允许有特殊字符，密码默认为用户名+手机号后3位；创建完用户后，密码通过邮箱发送给用户；用户必须关联具体的公司组织架构下。
  3、岗位：创建岗位时，需要同时给岗位赋予菜单权限、绑定归属区域、归属部门。
 四、项目管理
   项目管理记录的是对需求的分组，即，在进行需求流转时，可以指定某个角色必须选择具体的项目名称，绑定需求与项目的关系，用于需求的分析统计
   1、新建项目：必须记录项目的名称、归属公司、归属团队、负责人（关联用户管理中具体的人，可以跳转至该人的详情信息）、项目的起止时间，当项目到了截止时间，则该项目状态为截止，则无法在需求流转时选中。
   2、项目列表支持筛选，按照名称模糊筛选、按照状态、创建时间、截止时间等进行筛选过滤，支持对筛选结果进行excel的导出，支持项目的批量导入导出，需要制作导入模板文件。
 五、评审管理
   严格来讲，评审管理只是需求流转的中间状态，是分配给具体的角色或人员去进行流转的，不应该有这个菜单
 六、文档中心
  1、文档中心，支持对文档进行在线预览，也支持生成在线预览链接，将链接进行分享预览或生成二维码分享
  2、需求创建或流转过程中的附件，默认是加入到文档中心的，当来自需求流程中的文档被检索时，需要一并列出其关联的需求信息，需求标题、需求内容概要等信息。
  3、对文档进行检索时，要结合LLM大模型，输出检索的思维链及检索结果全过程，切记不能仅给出结果。